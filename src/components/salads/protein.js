/*
import createIngredient from './createIngredient';

const factoryArr = [
  ['chickpeas/garbanzo beans', 'one 14.5oz can'],
  ['', '8 cups'],
  ['green cabbage, or mixed', '6 cups'],
  ['iceberg lettuce', '8 cups'],
];

const saladProteins = factoryArr.map(createIngredient(i[0], i[1]));
console.log(saladProteins);

export default saladProteins;
*/
