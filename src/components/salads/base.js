/*
NEW FEATURE UNDER CONSTRUCTION

import createIngredient from './createIngredient';

const factoryArr = [
  ['spinach', '8 cups'],
  ['romaine lettuce', '8 cups'],
  ['green cabbage, or mixed', '6 cups'],
  ['iceberg lettuce', '8 cups'],
  ['cucumber', '4 cups'],
];

const saladBases = factoryArr.map(createIngredient(name, amount);
console.log(saladBases);

export default saladBases;
*/
